#include <iostream>

// A function definition has four elements: the return type, function name, parameter list, and function body.
// main function required to return an integer

int main(){
    std::cout << "Hello, World!" << std::endl;
    return -1;
}

// Typesm define both the contents of a data element and the operations that can be performed on it.

// You compile programs with clang++ using the following command: clang++ main.cpp -o main
// You run the compiled program with: ./main



